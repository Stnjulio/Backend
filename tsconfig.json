{
  "compilerOptions": {
    "target": "ES2016",                                /* Define a versão do JavaScript de saída. */
    "module": "commonjs",                              /* Módulo de importação/exportação, utilizado no Node.js. */
    "outDir": "./dist",                                /* Pasta de saída para arquivos compilados. */
    "rootDir": "./src",                                /* Diretório onde estão os arquivos fonte (TypeScript). */
    "esModuleInterop": true,                           /* Habilita interoperabilidade com módulos CommonJS. */
    "skipLibCheck": true,                              /* Ignora checagem de tipos nos arquivos .d.ts. */
    "forceConsistentCasingInFileNames": true,          /* Garante que o case (maiúsculas/minúsculas) seja consistente nos imports. */
    "strict": true                                     /* Habilita todas as verificações estritas de tipo. */
  },
  "include": [
    "src/**/*.ts"                                     /* Inclui todos os arquivos TypeScript dentro da pasta 'src'. */
  ],
  "exclude": [
    "node_modules"                                    /* Exclui a pasta 'node_modules' da compilação. */
  ]
}
