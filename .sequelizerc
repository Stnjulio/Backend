const path = require('path')

module.exports = {
  'config': path.resolve('./sequelize-config.js'), // Usa o arquivo JS
  'models-path': path.resolve('src/models'),
  'migrations-path': path.resolve('migrations'),
  'seeders-path': path.resolve('migrations'),
}